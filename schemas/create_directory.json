{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "create_directory",
  "title": "create_directory",
  "description": "Create a new directory (folder) at the specified path. Creates parent directories as needed.",
  "type": "object",
  "properties": {
    "path": {
      "type": "string",
      "description": "Absolute or ~-relative path for the new directory"
    },
    "exist_ok": {
      "type": "boolean",
      "default": true,
      "description": "If true, do not raise an error when the directory already exists"
    }
  },
  "additionalProperties": false,
  "required": ["path"]
}
